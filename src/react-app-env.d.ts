/// <reference types="react-scripts" />

interface UserCurrencyInfo {
  title: string,
  currency: string,
  ballance: number
}

interface Account {
  title: string,
  currency: string,
  balance: number,
}

interface CurrencyObj {
  currency: string,
  balance: number,
}